import React, { Component } from 'react'
import './main.css'

import Form from '../../Form'

class Main extends Component {
    render() {
        return (
            <main>
                <Form />
            </main>
        )
    }
}

export default Main
